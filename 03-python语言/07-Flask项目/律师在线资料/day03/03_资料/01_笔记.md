##### 1,前端文件运行

- 目的: 可以启动前端文件
- 操作流程:
  - 1, 使用vscode可以打开前端项目
  - 2, 安装扩展包
    - cnpm install
  - 3, 运行前端项目
    - npm run serve

##### 2,flask-cros跨域解决

- 目的: 可以解决跨域问题

- 复习跨域:

  - 协议, 端口, 域名; 只要有任何一个不一样, 就表示跨域了

- 操作流程:

  - 1, 安装扩展

    - pip install flask-cors

  - 2, 设置允许跨域(lawyer/__init__.py)

    - ```python
      #工厂方法
      def create_app():
          #1,创建app对象
          app = Flask(__name__)
      
          #1,1 设置允许跨域
          CORS(app)
          
          ...
      ```

##### 3,短信流程测试

- 目的: 可以通过前后端,发送短信测试

##### 4,注册流程测试

- 目的: 可以通过前后端测试,完成注册业务功能

##### 5,登录接口调试

- 目的: 可以调试前后端交互的登录流程

##### 6,首页视图-法律问题

- 目的:可以编写类视图,查询法律问题

- 操作流程:

  - 1, 子路由(lawyer/resources/question/__init__.py)

    - ```python
      from flask import Blueprint
      from flask_restful import Api
      from lawyer.resources.question import question
      
      #1,创建蓝图对象, api对象
      question_blue = Blueprint("question",__name__)
      question_api = Api(question_blue,catch_all_404s=True)
      
      #2,加载类视图
      question_api.add_resource(question.NewQuesionResource,"/v1_0/new_questions",endpoint="NewQuesionResource")
      ```

    - 2, 类视图(lawyer/resources/question/question.py)

      - ```python
        from flask_restful import Resource
        from flask_restful.reqparse import RequestParser
        from common.utils.slave_db_router import Queries
        
        class NewQuesionResource(Resource):
            def get(self):
                #1,解析参数
                rp = RequestParser()
                rp.add_argument("cur_page",type=int,required=True,location='args')
                rp.add_argument("per_page_count",type=int,required=True,location='args')
        
                #2,获取参数
                args = rp.parse_args()
                cur_page = args.cur_page
                per_page_count = args.per_page_count
        
                #3,查询法律问题
                sql = """select * from question_basic limit %s,%s;"""
                data = Queries.fetchall(sql,[(cur_page-1)*per_page_count,per_page_count])
        
                #4,组装数据
                dataList = []
                for item in data:
                    item_dict = {
                        "id":1,
                        "title":"法律问题",
                        "author_name":"老王"
                    }
                    dataList.append(item_dict)
        
                #5,返回响应
                return dataList
        ```

  - 3, 注册到app(lawyer/__init__.py)

    - ```python
      def create_app():
        	....
          #7,注册法律问题蓝图到app中
          from .resources.question import question_blue
          app.register_blueprint(question_blue)
      ```

##### 7,首页视图-数据获取

- 目的: 可以获取法律问题的数据展示在首页中

- 操作流程:

  - 1, 导入question模型类(common/models/question_answer.py)

  - 2, 封装获取question对象类(common/cache/question.py)

    - ```python
      from sqlalchemy.orm import load_only
      
      from common.models.question_answer import Question
      
      #1,获取question对象,缓存类
      class QuestionBasicCache(object):
      
          #1,初始化方法
          def __init__(self,id):
              self.id = id
      
          #2,获取对象
          def get(self):
              #1,根据id获取question对象
              obj = Question.query.options(load_only(Question.id,
                                               Question.title,
                                               Question.user_id,
                                               Question.expertise_id,
                                               Question.city_id,
                                               Question.ctime
                                               )).filter_by(id=self.id).first()
      
              #2,将question对象,转换成字典
              if not obj:
                  return None
      
              obj_dict = {
                  "id":obj.id,
                  "title":obj.title,
                  "user_id":obj.user_id,
                  "expertise":obj.expertise_id, #TODO 需要根据expertise_id获取expertise
                  "city":obj.city_id, #TODO 需要根据city_id获取city
                  "create_time":obj.ctime.strftime("%Y-%m-%d %H:%M%S"),
              }
      
              #3,返回
              return obj_dict
      ```

  - 3, 调用获取question对象(lawyer/resources/question/question.py)

    - ```python
      class NewQuesionResource(Resource):
          def get(self):
           		...
              
            	for item in data:
                  #4,1通过qust_id获取到question对象
                  question = QuestionBasicCache(item[0]).get()
                  
      						item_dict = {
                      "id":question["id"],
                      "title":question["title"],
                      "author_name":"老王",
                      "city":question["city"],
                      "create_time":question["create_time"],
                  }
      ```

##### 8,获取城市信息

- 目的: 可以获取城市的信息展示在首页中

- 操作流程:

  - 1, 编写城市模型类(common/models/city.py)

    - ```python
      from common.models import db
      
      #1,城市模型类
      class City(db.Model):
          __tablename__ = "areas"
      
          id = db.Column(db.Integer,primary_key=True,doc="城市id")
          name = db.Column(db.String(20),doc="城市名称")
          parent_id = db.Column("pid",db.Integer,db.ForeignKey("areas.id"),doc="父类id")
      
          #关系属性
          parent = db.relationship("City",remote_side=[id],backref = db.backref("childs",lazy="dynamic"))
      ```

  - 2, 调用(common/cache/question.py)

    - ```python
      class QuestionBasicCache(object):
      
          ...
      
          #2,获取对象
          def get(self):
             ...
              obj_dict = {
                  ...
                  "city":City.query.filter_by(id=obj.city_id).first().name, 						...
              }
         
      ```

##### 9, 法律问题类型

- 目的: 可以关键法律问题的id取出对应法律问题的类型

- 操作流程:

  - 1, 设置常量信息(common/cache/expertise.py)

    - ```python
      EXPERTISE = ((1, '不当竞争'), (2, '个人独资'), (3, '交通事故'), (4, '人身损害'), (5, '保险理赔'), (6, '债务追讨'), (7, '公司'), (8, '公司清算'), (9, '公司犯罪'), (10, '公安国安'), (11, '其它'), (12, '农村承包'), (13, '刑事'), (14, '刑事自诉'), (15, '刑事辩护'), (16, '劳动纠纷'), (17, '医疗纠纷'), (18, '取保候审'), (19, '合伙联营'), (20, '合同审查'), (21, '合同纠纷'), (22, '国家赔偿'), (23, '国际贸易'), (24, '婚姻家庭'), (25, '工伤赔偿'), (26, '工商查询'), (27, '工商税务'), (28, '工程建筑'), (29, '常年顾问'), (30, '广告宣传'), (31, '房产纠纷'), (32, '抵押担保'), (33, '拆迁安置'), (34, '招标投标'), (35, '新闻侵权'), (36, '期货交易'), (37, '民事'), (38, '水利电力'), (39, '求学教育'), (40, '污染损害'), (41, '消费权益'), (42, '涉外'), (43, '环境污染'), (44, '电信通讯'), (45, '知识产权'), (46, '私人律师'), (47, '经济'), (48, '经济仲裁'), (49, '经销代理'), (50, '网络法律'), (51, '股份转让'), (52, '行政诉讼'), (53, '调解谈判'), (54, '资信调查'), (55, '连锁加盟'), (56, '遗产继承'), (57, '金融证券'), (58, '银行保险'))
      ```

  - 2, 根据id加载常量信息(common/cache/question.py)

    - ```python
      class QuestionBasicCache(object):
      
          ...
      
          #2,获取对象
          def get(self):
             ...
              obj_dict = {
                  ...
                  "expertise": EXPERTISE[obj.expertise_id-1][1] if obj.expertise_id else None, #TODO 需要根据expertise_id获取expertise
              }
         
      ```

##### 10, 首页显示用户名

- 目的: 可以将用户名称显示首页

- 操作流程:

  - 1, 查询用户的名称(common/cache/question.py)

    - ```python
      class QuestionBasicCache(object):
      
          ...
      
          #2,获取对象
          def get(self):
             ...
              obj_dict = {
                  ...
                  "author_name":User.query.filter(User.id == obj.user_id).first().name,
              }
         
      ```

    - 

  - 2, 返回用户名, 回答数量

    - ```python
      class NewQuesionResource(Resource):
          def get(self):
              ...
              
              #4,组装数据
              dataList = []
              for item in data:
                	...
      
                  item_dict = {
                      ...
                      "author_name":question["author_name"],
                      "answer_count":0
      
                      ...
                  }
                  dataList.append(item_dict)
      
              #5,返回响应
              return dataList
      ```

##### 11,我要提问-类视图

- 目的: 可以编写类视图,处理用户提交请求

- 操作流程:

  - 1, 子路由(lawyer/resources/question/__init__.py)

    - ```python
      question_api.add_resource(question.QuesionResource,"/v1_0/question",endpoint="QuesionResource") #提交问题
      ```

  - 2, 类视图(lawyer/resources/question/question.py)

    - ```python
      #2,提交问题
      class QuesionResource(Resource):
          def post(self):
              return {"message":"OK"}
      ```

##### 12,省份信息获取

- 目的: 可以编写类视图获取省份信息

- 操作流程:

  - 1, 子路由(lawyer/resources/common/__init__.py)

    - ```python
      from flask import Blueprint
      from flask_restful import Api
      from lawyer.resources.common import common
      
      #1,创建common蓝图对象, api对象
      common_blue = Blueprint("common",__name__)
      common_api = Api(common_blue,catch_all_404s=True)
      
      #2,将蓝图对象,添加到api中
      common_api.add_resource(common.ProvinceResource,'/v1_0/common/provinces',endpoint="ProvinceResource")
      ```

      

  - 2, 类视图(lawyer/resources/common/common.py)

    - ```python
      from flask_restful import Resource
      from common.models.city import City
      
      #1,获取省的信息
      class ProvinceResource(Resource):
          def get(self):
              #1,查询所有省的信息
              cities = City.query.filter(City.parent_id==None).all()
      
              #2,数据拼接
              data_list = []
              for city in cities:
                  city_dict = {
                      "province_id":city.id,
                      "province_name":city.name
                  }
                  data_list.append(city_dict)
      
              #3,返回响应
              return data_list
      ```

  - 3, 注册省份蓝图到app中(lawyer/__init__.py)

    - ```python
      #工厂方法
      def create_app():
        	...
          
          #8,注册省份信息蓝图到app中
          from .resources.common import common_blue
          app.register_blueprint(common_blue)
      ```

##### 13,获取城市信息

- 目的: 可以根据省份信息,获取对应的城市信息

- 操作流程:

  - 1, 子路由(lawyer/resources/common/__init__.py)

    - ```python
      common_api.add_resource(common.CityResource,'/v1_0/common/cities',endpoint="CityResource")
      ```

      

  - 2, 类视图(lawyer/resources/common/common.py)

    - ```python
      #2,获取市的信息
      class CityResource(Resource):
          def get(self):
              #1,解析参数
              rp = RequestParser()
              rp.add_argument("pid",type=int,required=True,location="args")
      
              #2,获取参数
              args = rp.parse_args()
              pid = args.pid
      
              #3,查询市信息
              cities = City.query.filter(City.parent_id==pid).all()
              print(cities)
      
              #4,拼接数据
              city_list = []
              for city in cities:
                  city_dict = {
                      "city_id":city.id,
                      "city_name":city.name
                  }
                  city_list.append(city_dict)
      
              #5,返回响应
              return city_list
      ```

      

##### 14,获取区域信息

- 目的: 可以根据选择的城市信息,获取区域信息

- 操作流程:

  - 1, 子路由(lawyer/resources/common/__init__.py)

    - ```python
      common_api.add_resource(common.DistrictResource,'/v1_0/common/districts',endpoint="DistrictResource")
      ```

  - 2, 类视图(lawyer/resources/common/common.py)

    - ```python
      #3,获取区域信息
      class DistrictResource(Resource):
          def get(self):
              #1,解析参数
              rp = RequestParser()
              rp.add_argument("pid",type=int,required=True,location="args")
      
              #2,获取参数
              args = rp.parse_args()
              pid = args.pid
      
              #3,获取区域信息
              districts = City.query.filter(City.parent_id==pid).all()
      
              #4,拼接数据
              district_list = []
              for district in districts:
                  district_dict = {
                      "district_id":district.id,
                      "district_name":district.name
                  }
                  district_list.append(district_dict)
      
              #5,返回响应
              return district_list
      ```

