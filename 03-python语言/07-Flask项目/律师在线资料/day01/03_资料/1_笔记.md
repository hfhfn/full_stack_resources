##### 1,项目介绍

- 目的: 知道项目业务
- 操作流程:
  - 业务:
    - 1, 普通用户端
    - 2, 律师端
    - 3, 后台系统
  - 开发方式:
    - 1, 前后端分离
    - 2, 技术栈(flask + vue)

##### 2, 项目创建

- 目的: 在码云创建项目, 并使用git进行管理
- 操作流程:
  - 1, 在码云创建项目,并clone到本地
  - 2, 使用pycharm打开,创建laywer包并提交

##### 3, 入口文件启动项目

- 目的: 创建flask项目, 编写main启动模块
- 操作流程:
  - 1, 更改包的名字为lawyer
  - 2, 创建main.py入口文件(lawyer/main.py)
  - 3, 设置运行方式
    - export FLASK_APP=lawyer.main
    - python3 -m flask run
    - 开发模式设置: FLASK_ENV=development

##### 4, 配置文件

- 目的: 创建配置文件,抽取配置

- 操作流程:

  - 1, 封装工厂方法create_app(lawyer/init.py)	

    - ```python
      #工厂方法
      def create_app():
        pass
      ```

  - 2, 配置项目配置信息(common/settings/dafult.py)

    - ```python
      
      #1,默认配置文件
      class DefaultConfig(object):
          #1,秘钥配置
          SECRET_KEY = "JFKDJFKDF"
      
      #2,开发模式配置
      class DevelopConfig(object):
          pass
      
      #3,生产模式配置
      class ProductConfig(object):
          pass
      
      #4,入口访问字典
      config_dict = {
          "development":DevelopConfig,
          "production":ProductConfig,
      }
      ```

  - 3, 加载配置信息(lawyer/init.py)

    - ```python
      #工厂方法
      def create_app():
          #1,创建app对象
          app = Flask(__name__)
      
          #2,加载配置信息
          config = config_dict.get(app.config.get("ENV"))
          app.config.from_object(config)
      
          #3,配置数据库连接信息
          return app
      ```

##### 5, 数据库操作

- 目的: 配置数据库连接,并进行测试

- 操作流程:

  - 1, 导入模型类(common/models)

  - 2, 修改配置文件,配置数据库信息(common/settings/default.py)

    - ```python
      
      #1,默认配置文件
      class DefaultConfig(object):
          #1,秘钥配置
          SECRET_KEY = "JFKDJFKDF"
      
          #2,数据库配置
          SQLALCHEMY_DATABASE_URI = "mysql+pymysql://root:mysql@172.16.12.134/lawyer_ol"
          SQLALCHEMY_TRACK_MODIFICATIONS = False
      
      #2,开发模式配置
      class DevelopConfig(DefaultConfig):
          DEBUG = True
      
      #3,生产模式配置
      class ProductConfig(DefaultConfig):
          DEBUG = False
      
      #4,入口访问字典
      config_dict = {
          "development":DevelopConfig,
          "production":ProductConfig,
      }
      ```

  - 3, 加载数据库配置信息(lawyer/init.py)

    - ```python
      def create_app():
          ...
      
          #3,配置数据库连接信息
          from common.models import db
          db.init_app(app)
          return app
      ```

##### 6, enter&exit

- 目的: 知道enter&exit方法

- 操作流程:

  - 1, enter: 当使用with执行方法的时候, 优先调用

  - 2, exit: 当with方法执行完成之后, 执行exit方法

  - 3, 代码:

    - ````python
      In [1]: class Person(object): 
         ...:     def __enter__(self): 
         ...:         print("this is enter") 
         ...:     def __exit__(self,type,value,trace): 
         ...:         print("this is exit")  
         ...:                                                                                                                                                            
      
      In [2]: with Person() as p: 
         ...:     print("test") 
         ...:                                                                                                                                                            
      this is enter
      test
      this is exit
      
      
      ````

##### 7,数据库主从配置

- 目的: 数据库主从关系配置
- 操作流程:
  - 查看笔记中的<<主从配置文档>>

##### 8, 数据库从操作

- 目的: 数据库注册配置

- 操作流程:

  - 1, 定义路由配置类(common/utils/slave_db_router.py)

    - ```python
      from pymysql import connect
      from flask import current_app
      
      #1,上下文管理器,配合with使用
      class DBOpenExe(object):
          #1,配置初始化信息
          def __init__(self):
              self.conn = connect(
                  host=current_app.config.get("SLAVE_HOST"),
                  port=current_app.config.get("SLAVE_PORT"),
                  user=current_app.config.get("SLAVE_USER"),
                  password=current_app.config.get("SLAVE_PASSWORD"),
                  database=current_app.config.get("SLAVE_DATABASE"),
                  charset=current_app.config.get("SLAVE_CHARSET"),
              )
              self.cs = self.conn.cursor()
      
          #2,创建连接之前调用
          def __enter__(self):
              return self.cs
      
          #3,连接完成调用
          def __exit__(self, *args):
              self.conn.commit()
              self.cs.close()
              self.conn.close()
      ```

  - 2, 修改公共的配置文件(common/settings/default.py)

    - ```python
      #1,默认配置文件
      class DefaultConfig(object):
          ...
          #3,数据库主从配置
          SLAVE_HOST = "172.16.12.134"
          SLAVE_PORT = 8306
          SLAVE_USER = "root"
          SLAVE_PASSWORD = "mysql"
          SLAVE_DATABASE = "lawyer_ol"
          SLAVE_CHARSET = "utf8"
      ```

  - 3, 在入口文件进行测试

    - ```python
      @app.route('/')
      def hello_world():
          #1,数据库测试
          ...
          
          #2,数据库从的测试
          with DBOpenExe() as cs:
              sql = """select * from user_basic;"""
              cs.execute(sql)
              data = cs.fetchall()
          print(data)
      
          return "helloworld",200
      ```

##### 9, redis集群配置

- 目的: 配置redis集群
- 操作流程:
  - 见<<redis配置文档>>

##### 10, redis哨兵配置

- 目的: 可以配置哨兵用于监控主从运行情况
- 操作流程:
  - 见<<redis配置文档>>
- 注意点:
  - 如果需要在外界连接redis需要将启动的配置host改为:0.0.0.0即可

##### 11, 日志配置

- 目的: 可以配置日志信息, 实现调试信息记录

- 操作流程:

  - 1, 配置日志方法(common/utils.logger.py)

    - ```python
      import logging
      from logging.handlers import RotatingFileHandler
      
      def log_file(log_lv):
          """记录日志信息"""
          # 设置哪些日志信息等级要被记录
          logging.basicConfig(level=log_lv)  # 调试debug级
          # 创建日志记录器，指明日志保存的路径、每个日志文件的最大大小、保存的日志文件个数上限
          file_log_handler = RotatingFileHandler("/Users/heJing/Desktop/律师项目/day01/02_代码/lawyer_backend/logs/log", maxBytes=1024 * 1024 * 100, backupCount=10)
          # 创建日志记录的格式 日志等级 输入日志信息的文件名 行数 日志信息
          formatter = logging.Formatter('%(levelname)s %(filename)s:%(lineno)d %(message)s')
          # 为刚创建的日志记录器设置日志记录格式
          file_log_handler.setFormatter(formatter)
          # 为全局的日志工具对象（flask app使用的）添加日志记录器
          logging.getLogger().addHandler(file_log_handler)
      ```

  - 2, 调用日志方法(lawyer/init.py)

    - ```python
      #工厂方法
      def create_app():
          #1,创建app对象
      		...
      
          #1,1 记录日志信息
          log_file(logging.ERROR)
          
          ...
      ```

  - 3,测试日志输出(lawyer/main.py)

    - ```python
      @app.route('/')
      def hello_world():
          ...
      
          #3,测试日志记录信息
          logging.debug("这是日志信息debug")
          logging.error("这是日志信息error")
      ```

- 注意点:
  - 1, 日志的登记有大小,共6个
  - 2, 如果设置的级别为A,那么只有大于等于A级别的日志才会输出

##### 12,日志配置优化

- 目的: 可以通过配置, 日志的登记随着项目运行方式的变化而变化

- 操作流程:

  - 1, 设置配置文件(common/settings/defalut.py)

    - ```python
      #1,默认配置文件
      class DefaultConfig(object):
          ...
          
          #4,默认日志级别
          LOG_LV = logging.DEBUG
      
      #3,生产模式配置
      class ProductConfig(DefaultConfig):
      		...
          LOG_LV = logging.ERROR
      
      ```

  - 2, 调用的时候,传入配置(lawyer/init.py)

    - ```python
      def create_app():
          #1,创建app对象
      		...
      
          #2,加载配置信息
          config = config_dict.get(app.config.get("ENV"))
          app.config.from_object(config)
      
          #2,1 记录日志信息
          log_file(config.LOG_LV)
      ```

##### 13,集群主从测试

- 目的:操作流程:

  - 1, 修改配置文件(common/settings/utils.py)

    - ```python
      #1,默认配置文件
      class DefaultConfig(object):
          ...
      
          #5,redis集群
          REDIS_CLUSTER = [
              {"host":"172.16.12.134","port":"7000"},
              {"host":"172.16.12.134","port":"7001"},
              {"host":"172.16.12.134","port":"7002"},
          ]
      
          #6,redis主从
          REDIS_SENTINELS = [
              ("172.16.12.134", "16380"),
              ("172.16.12.134", "16381"),
              ("172.16.12.134", "16382"),
          ]
          REDIS_SENTINEL_SERVICES_NAME = "mymaster"
      ```

  - 2, 加载配置(lawyer/init.py)

    - ```python
      #工厂方法
      def create_app():
          ...
          
          #4,redis集群加载
          from rediscluster import StrictRedisCluster
          app.redis_cluster = StrictRedisCluster(startup_nodes=app.config.get("REDIS_CLUSTER"))
      
          #5,redis哨兵,主从配置
          from redis.sentinel import Sentinel
          sentinel = Sentinel(app.config.get("REDIS_SENTINELS"))
          app.redis_master = sentinel.master_for(app.config.get("REDIS_SENTINEL_SERVICES_NAME"))
          app.redis_slave = sentinel.slave_for(app.config.get("REDIS_SENTINEL_SERVICES_NAME"))
      
          return app
      ```

  - 3, 测试(lawyer/main.py)

    - ```python
      @app.route('/')
      def hello_world():
          ...
      
          #4,集群测试
          # current_app.redis_cluster.set("name","helloworld")
          # print(current_app.redis_cluster.get("name"))
      
          #5,测试主从
          current_app.redis_master.set("age","10")
          print(current_app.redis_master.get("age"))
      
          return "helloworld",200
      ```

##### 14, 用户蓝图创建

- 目的: 可以创建用户蓝图,并注册到app中

- 操作流程:

  - 1, 创建用户蓝图(lawyer/resources/users/init.py)

    - ```python
      from flask import Blueprint
      from flask_restful import Api
      from lawyer.resources.users import passport
      
      #1,创建蓝图对象
      user_blue = Blueprint("user",__name__)
      
      #2,创建api对象,关联蓝图
      user_api = Api(user_blue,catch_all_404s=True)
      
      #3,添加路由资源到api对象中
      user_api.add_resource(passport.NewClientResource,"/v1_0/new_client",endpoint="NewClient")
      
      ```

  - 2, 创建类视图(lawyer/resources/users/passport.py)

    - ```python
      from flask_restful import Resource
      
      class NewClientResource(Resource):
          def get(self):
              return {"name":"zhangsan"},200
      
      class AuthorizationResource(Resource):
          pass
      ```

  - 3, 注册蓝图到app中(lawyer/init.py)

    - ```python
      #工厂方法
      def create_app():
          ...
          
          #6,注册用户蓝图到app中
          from .resources.users import user_blue
          app.register_blueprint(user_blue)
      
          return app
      ```

  - 4, 查看所有的路由

    - ```python
      if __name__ == '__main__':
          print(app.url_map)
      		...
      ```