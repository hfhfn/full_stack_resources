# 项目实现全文检索接口

[TOC]

<!-- toc -->

## 1. `elasticsearch python`客户端使用

> - 文档<https://elasticsearch-py.readthedocs.io/en/master/>
>
> - 安装`pip install elasticsearch`
>
>   - 包含es所有版本，使用不同版本的es导入方式不同
>
> - 使用方法
>
>   > - 创建elasticsearch客户端
>   >
>   >   ```python
>   >   from elasticsearch5 import Elasticsearch
>   >   
>   >   # elasticsearch集群服务器的地址
>   >   ES = [
>   >       '127.0.0.1:9200'
>   >   ]
>   >   
>   >   # 创建elasticsearch客户端
>   >   es = Elasticsearch(
>   >       ES,
>   >       # 启动前嗅探es集群服务器
>   >       sniff_on_start=True,
>   >       # es集群服务器结点连接异常时是否刷新es结点信息
>   >       sniff_on_connection_fail=True,
>   >       # 每60秒刷新结点信息
>   >       sniffer_timeout=60
>   >   )
>   >   ```
>   >
>   > - 搜索使用方式
>   >
>   >   ```python
>   >   query = {
>   >       'query': {
>   >           'bool': {
>   >               'must': [
>   >                   {'match': {'_all': 'python web'}}
>   >               ],
>   >               'filter': [
>   >                   {'term': {'status': 2}}
>   >               ]
>   >           }
>   >       }
>   >   }
>   >   ret = es.search(index='articles', doc_type='article', body=query)
>   >   ```

## 2. 头条项目搜索接口视图实现

> - 需求：输入关键词返回相关性较高的文章id
>
> - 在`toutiao-backend/toutiao/resources/__init__.py`中，给app对象添加es连接对象
>
>   > ```python
>   >    # Elasticsearch
>   >    app.es = Elasticsearch(
>   >        app.config['ES'],
>   >        # sniff before doing anything
>   >        sniff_on_start=True,
>   >        # refresh nodes after a node fails to respond
>   >        sniff_on_connection_fail=True,
>   >        # and also every 60 seconds
>   >        sniffer_timeout=60
>   >    )
>   > ```
>
> - 在toutiao-backend/toutiao/resources/search目录中新建search.py
>
>   > ```python
>   > from flask import current_app
>   > from flask_restful import inputs, Resource
>   > from flask_restful.reqparse import RequestParser
>   > 
>   > 
>   > class SearchResource(Resource):
>   >    """
>   >    #1. 获取参数 q搜索关键词（必要） page页号 per_page每页数量
>   >    #2. 确定 age页号 per_page每页数量 的值
>   >    #3. 构造es的query查询字典
>   >    #4. 查询获取结果
>   >    #5. 取出查询的文章id并返回
>   >    """
>   >    def get(self):
>   >        qs_parser = RequestParser()
>   >        qs_parser.add_argument('q', type=inputs.regex(r'^.{1,50}$'), required=True, location='args')  # 搜索关键词 限制输入1-50个字符
>   >        qs_parser.add_argument('page', type=inputs.positive, required=False, location='args')  # 页号，限制整数自然数
>   >        qs_parser.add_argument('per_page', # 每页多少个
>   >                               required=False,
>   >                               location='args',
>   >                               type=inputs.int_range(1, 20, 'per_page'))
>   >        args = qs_parser.parse_args()
>   >        q = args.q
>   >        page = 1 if args.page is None else args.page
>   >        per_page = 10 if args.per_page is None else args.per_page
>   >        """curl -X GET 127.0.0.1:9200/articles/article/_search?pretty -d'
>   >        {"from": 1, "size": 10, "_source": ["title", "article_id"], "query": {
>   >                "bool": {"must": [{"match": {"_all": "python"}}],
>   >                         "filter": [{"term": {"status": 2}}]}}}'"""
>   >        query = {
>   >            'from': (page-1)*per_page, # 从第几条开始
>   >            'size': per_page, # 一共返回多少条
>   >            '_source': ['title', 'article_id'],  # 指定要返回的字段
>   >            'query': {
>   >                'bool': {
>   >                    'must': [
>   >                        {'match': {'_all': q}} # 全文检索
>   >                    ],
>   >                    'filter': [
>   >                        {'term': {'status': 2}} # 审核状态必须是2 过审
>   >                    ]
>   >                }
>   >            }
>   >        }
>   > 
>   >        es_ret = current_app.es.search(index='articles', doc_type='article', body=query) # 返回dict
>   >        results = [i['_source'] for i in es_ret['hits']['hits']]
>   >        return {'page': page, 'per_page': per_page,
>   >                'results': results, 'total_count': es_ret['hits']['total']}
>   > ```
>
> - 在`/resources/search/__init__.py`中注册视图
>
>   > ```python
>   > from . import search
>   > search_api.add_resource(search.SearchResource, '/v1_0/search',
>   >                        endpoint='Search')
>   > ```

## 3. 测试接口

> ```python
> import requests
> url = 'http://192.168.45.128:5000/v1_0/search?q=python&page=5&per_page=3'
> resp = requests.get(url)
> print(resp.json())
> ```

## 4. 添加ES新文章索引数据

> 如果要向es库中添加新文章的索引，方法如下（比如在自媒体平台发布文章存在该需求）
>
> ```python
> doc = {
>           'article_id': article.id,
>           'user_id': article.user_id,
>           'title': article.title,
>           'content': article.content.content,
>           'status': article.status,
>           'create_time': article.ctime
>       }
> current_app.es.index(index='articles', doc_type='article', body=doc, id=article.id)
> ```

